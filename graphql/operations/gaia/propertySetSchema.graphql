query propertySetSchema($ownerKey: String!, $name: String!) {
  gaiaQueries {
    propertySetSchema(ownerKey: $ownerKey, name: $name) {
      id
      ownerKey
      name
      properties
      configurable
      status
    }
  }
}
