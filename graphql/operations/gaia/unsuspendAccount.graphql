mutation unsuspendAccount($id: ID!) {
  gaiaMutations {
    unsuspendAccount(id: $id) {
      ... on QuerySuccess {
        __typename
        message
        id
      }
      ... on StandardError {
        __typename
        message
      }
    }
  }
}
