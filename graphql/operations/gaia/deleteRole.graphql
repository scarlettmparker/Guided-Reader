mutation deleteRole($id: ID!) {
  gaiaMutations {
    deleteRole(id: $id) {
      ... on QuerySuccess {
        __typename
        message
        id
      }
      ... on StandardError {
        __typename
        message
      }
    }
  }
}
