query viewedTexts($pagination: PaginationInput) {
  hadesQueries {
    viewedTexts(pagination: $pagination) {
      items {
        textId
        viewedAt
      }
      pageInfo {
        page
        size
        totalPages
        totalCount
        hasNextPage
        hasPreviousPage
      }
    }
  }
}
