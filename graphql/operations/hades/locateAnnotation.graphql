query locateAnnotation($id: ID!) {
  hadesQueries {
    annotation(id: $id) {
      id
      positionId
      body
      status
      upvotes
      downvotes
      netScore
      createdAt
      updatedAt
    }
  }
}
