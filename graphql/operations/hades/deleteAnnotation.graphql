mutation deleteAnnotation($id: ID!) {
  hadesMutations {
    deleteAnnotation(id: $id) {
      ... on QuerySuccess {
        __typename
        message
        id
      }
      ... on StandardError {
        __typename
        message
      }
    }
  }
}
