query listComments($annotationId: ID!, $includeHidden: Boolean, $pagination: PaginationInput) {
  hadesQueries {
    comments(annotationId: $annotationId, includeHidden: $includeHidden, pagination: $pagination) {
      items {
        id
        annotationId
        parentId
        body
        status
        upvotes
        downvotes
        netScore
        author {
          type
          id
        }
        authorProfile {
          id
          discordId
          discordUsername
          globalName
          avatar
          cefrLevel
        }
        myVote
        createdAt
        updatedAt
      }
      pageInfo {
        page
        size
        totalPages
        totalCount
        hasNextPage
        hasPreviousPage
      }
    }
  }
}
