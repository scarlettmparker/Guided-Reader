mutation createText($input: ReaderTextInput!) {
  hadesMutations {
    createText(input: $input) {
      ... on QuerySuccess {
        __typename
        message
        id
      }
      ... on StandardError {
        __typename
        message
      }
    }
  }
}
