query locateText($id: ID!) {
  hadesQueries {
    text(id: $id) {
      id
      title
      content
      language
      level
      type
      sourceId
      status
      createdAt
      updatedAt
    }
  }
}
