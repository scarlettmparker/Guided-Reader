mutation addComment($input: CommentInput!) {
  hadesMutations {
    addComment(input: $input) {
      ... on QuerySuccess {
        __typename
        message
        id
      }
      ... on StandardError {
        __typename
        message
      }
    }
  }
}
