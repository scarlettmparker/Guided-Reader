mutation editText($id: ID!, $input: ReaderTextInput!) {
  hadesMutations {
    editText(id: $id, input: $input) {
      ... on QuerySuccess {
        __typename
        message
        id
      }
      ... on StandardError {
        __typename
        message
      }
    }
  }
}
